{
  "name": "concurrent",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run build && npm run\"",
    "build": "cd frontend && vite build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.1.0"
  },
  "engines" : { 
    "npm" : "8.x",
    "node" : "17.x"
  }
}
